# 真机验证 workflow

这个 workflow 统一真机验证时的账号顺序、最小探测步骤和写后回归口径。

## 1. 验证账号

如果用户给出目标机器并要求真机验证，只使用用户明确提供或当前环境已配置的验证账号。

1. 管理员验证：使用用户指定的管理员账号
2. 非管理员验证：使用用户指定的非管理员账号
3. 如需创建临时测试用户，先确认账号名、初始密码和清理方式

不要在 skill 产物中记录或传播测试账号密码。如果没有可用账号或登录失败，不要继续猜其他账号，应先向用户确认。

## 2. 固定先做的最小探测

开始任何写操作前，先做最小只读探测：

1. `trim-cli --host <host> --port <port> login`
2. 按目标模块执行 1 到 2 个只读命令确认连接和权限
3. 如果是文件相关，优先用 `file ls` 或对应只读搜索
4. 如果是存储相关，优先用 `storage pools` / `storage disks`
5. 如果是 Docker 相关，优先用 `docker stats` 或 `docker container ls`

目的：

- 确认连接目标正确
- 确认 session 已建立
- 确认当前账号权限是否符合本次操作

## 3. 写操作执行顺序

建议顺序：

1. 先做与目标最接近的只读探测
2. 再做单一、可解释、可回滚的写操作
3. 写完立即补一条只读命令确认结果
4. 如有临时资源，按需要清理

不要在同一轮验证里串多个不相关写操作。

## 4. 写后回归

至少补以下一种回归：

- 文件：重新 `ls` / `search` / `share list`
- 存储：重新 `pools` / `disks` / `removable`
- Docker：重新 `image ls` / `container ls` / `compose ls`
- 用户：重新 `user list` / `group list`

如果写前和写后状态无法客观对比，不应宣称验证通过。

## 5. 什么时候必须停下来确认

- 登录失败且当前账号不在默认顺序内
- 用户没给目标主机，但要求真机验证
- 写操作可能影响生产数据或正在运行的服务
- 实际返回与文档约束明显不一致，怀疑设备版本差异

## 6. 下一步

- 验证命令方向已明确：回对应模块 reference 看字段和端点
- 账号、权限或设备状态不明：先停下来补信息，不要继续猜
