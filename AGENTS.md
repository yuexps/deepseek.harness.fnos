# 项目智能体规范 (AGENTS.md)

## 1. 语言与注释
- 所有回复、文档、任务清单使用中文。
- 注释只写核心功能与业务用途，直接说明“做什么”，简洁、按需。
- 不注释显而易见代码；不写动机、历史。
- 禁止：个人情绪、过期注释、废话。

## 2. 代码规范
- 最小实现，遵循 KISS、YAGNI；只做当前需求。
- 遵循 DRY：保持 package main 扁平组织；通用纯函数抽离为内部 helper，不随意新增子包。
- 保持上游纯净：仅修改本宿主程序及打包资源，严禁修改上游 DSH 原生运行时代码。
- 单一职责；函数建议 ≤ 50 行，嵌套 ≤ 3 层，参数 ≤ 4 个。
- 禁止：无意义函数、未使用代码、过度设计、复制粘贴、魔法值、空异常、模糊命名。
- 命名：变量用名词，函数用动词，布尔用 `is/has/can/should`。
- 纯逻辑与 IO、网络、文件、UI 分离；资源必须释放。
- 外部输入必须校验；不硬编码密钥；日志分级且不泄露敏感信息。
- 性能优化必须有依据，优先保证正确、清晰。

## 3. 测试与交付
- **智能体不运行测试，不执行测试命令。测试只能由用户手动完成。**
- 修改后智能体只做代码级自检：语法、结构、命名、异常、资源、边界阅读检查。
- 除非用户明确要求，不新增测试文件、不生成测试脚本、不要求安装测试依赖。
- 交付时说明：改动了什么、如何实现、用户需手动验证哪些场景。
- 智能体流程：明确需求 → 复用现有 → 最小改动 → 按需抽离 → 代码自检 → 简述改动与手动验证点。
- 交付只输出必要内容，不扩大范围。

## 4. 提交前自检
- [ ] 只实现当前需求
- [ ] 无未使用代码、无重复、无无意义函数
- [ ] 命名清晰、职责单一、异常完整、资源释放
- [ ] 已做代码级自检，测试交由用户手动完成

